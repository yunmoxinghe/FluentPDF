<Page
    x:Class="FluentPDF.Pages.PdfViewerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentPDF.Pages"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <CommandBar Grid.Row="0"
                    x:Name="Toolbar"
                    DefaultLabelPosition="Right"
                    Background="Transparent"
                    HorizontalAlignment="Left">
            <AppBarButton x:Name="ZoomOutButton" Icon="ZoomOut" x:Uid="ZoomOut" Click="ZoomOutButton_Click"/>
            <AppBarButton x:Name="ZoomInButton"  Icon="ZoomIn"  x:Uid="ZoomIn"  Click="ZoomInButton_Click"/>
            <AppBarButton x:Name="ZoomFitButton" Icon="BackToWindow" x:Uid="ZoomFit" Click="ZoomFitButton_Click"/>
        </CommandBar>

        <!-- 加载进度环 -->
        <ProgressRing x:Name="LoadingRing" Grid.Row="1"
                      IsActive="False" Width="48" Height="48"
                      HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <!-- PDF 内容 -->
        <ScrollViewer x:Name="PdfScrollViewer" Grid.Row="1"
                      Visibility="Collapsed"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto"
                      ZoomMode="Enabled"
                      MinZoomFactor="0.25"
                      MaxZoomFactor="4.0"
                      SizeChanged="PdfScrollViewer_SizeChanged">
            <Grid x:Name="PagesHost">
                <ItemsControl x:Name="PagesControl" HorizontalAlignment="Center">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="local:PdfPageItem">
                            <Grid Margin="8,8,8,8"
                                  Width="{x:Bind DisplayWidth, Mode=OneWay}"
                                  Height="{x:Bind DisplayHeight, Mode=OneWay}"
                                  Background="{ThemeResource SystemControlBackgroundBaseLowBrush}">
                                <ProgressRing IsActive="{x:Bind IsFirstLoad, Mode=OneWay}"
                                              Width="32" Height="32"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                <Image Source="{x:Bind PageImage, Mode=OneWay}"
                                       Stretch="Fill"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
