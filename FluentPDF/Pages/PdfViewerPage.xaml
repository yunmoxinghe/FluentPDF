<Page
    x:Class="FluentPDF.Pages.PdfViewerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentPDF.Pages"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Background="Transparent">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Default">
                    <AcrylicBrush x:Key="CatalogPaneAcrylicBrush"
                                  BackgroundSource="Backdrop"
                                  TintColor="#202020"
                                  TintOpacity="0.72"
                                  TintLuminosityOpacity="0.85"
                                  FallbackColor="#202020"/>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Light">
                    <AcrylicBrush x:Key="CatalogPaneAcrylicBrush"
                                  BackgroundSource="Backdrop"
                                  TintColor="#F9F9F9"
                                  TintOpacity="0.78"
                                  TintLuminosityOpacity="0.9"
                                  FallbackColor="#F3F3F3"/>
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                    <SolidColorBrush x:Key="CatalogPaneAcrylicBrush"
                                     Color="{ThemeResource SystemColorWindowColor}"/>
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>

        <!--
            UWP ScrollViewer 默认模板中的 ScrollingIndicatorStates 状态机
            会在鼠标离开后把滚动条切回细条状 TouchIndicator，导致 Visible 属性失效。
            此 Style 覆盖模板，将两个状态都固定为 MouseIndicator，
            使滚动条始终以完整可交互形式显示。
        -->

        <!-- 紧凑正方形 TextBox：覆盖默认模板，去除 MinHeight 和多余 Padding -->
        <Style x:Key="CompactSquareTextBoxStyle" TargetType="TextBox">
            <Setter Property="MinWidth"          Value="0"/>
            <Setter Property="MinHeight"         Value="0"/>
            <Setter Property="Width"             Value="32"/>
            <Setter Property="Height"            Value="32"/>
            <Setter Property="Foreground"        Value="{ThemeResource TextControlForeground}"/>
            <Setter Property="Background"        Value="{ThemeResource TextControlBackground}"/>
            <Setter Property="BorderBrush"       Value="{ThemeResource TextControlBorderBrush}"/>
            <Setter Property="BorderThickness"   Value="{ThemeResource TextControlBorderThemeThickness}"/>
            <Setter Property="FontFamily"        Value="{ThemeResource ContentControlThemeFontFamily}"/>
            <Setter Property="FontSize"          Value="{ThemeResource ControlContentThemeFontSize}"/>
            <Setter Property="SelectionHighlightColor" Value="{ThemeResource TextControlSelectionHighlightColor}"/>
            <Setter Property="ScrollViewer.HorizontalScrollMode"        Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollMode"          Value="Auto"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Hidden"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility"   Value="Hidden"/>
            <Setter Property="ScrollViewer.IsDeferredScrollingEnabled"  Value="False"/>
            <Setter Property="Padding"           Value="4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Grid.Resources>
                                <Style x:Name="DeleteButtonStyle" TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Grid x:Name="ButtonLayoutGrid"
                                                      BorderBrush="{ThemeResource TextControlButtonBorderBrush}"
                                                      BorderThickness="{TemplateBinding BorderThickness}"
                                                      Background="{ThemeResource TextControlButtonBackground}">
                                                    <VisualStateManager.VisualStateGroups>
                                                        <VisualStateGroup x:Name="CommonStates">
                                                            <VisualState x:Name="Normal"/>
                                                            <VisualState x:Name="PointerOver">
                                                                <Storyboard>
                                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ButtonLayoutGrid" Storyboard.TargetProperty="Background">
                                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlButtonBackgroundPointerOver}"/>
                                                                    </ObjectAnimationUsingKeyFrames>
                                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="GlyphElement" Storyboard.TargetProperty="Foreground">
                                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlButtonForegroundPointerOver}"/>
                                                                    </ObjectAnimationUsingKeyFrames>
                                                                </Storyboard>
                                                            </VisualState>
                                                            <VisualState x:Name="Pressed">
                                                                <Storyboard>
                                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ButtonLayoutGrid" Storyboard.TargetProperty="Background">
                                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlButtonBackgroundPressed}"/>
                                                                    </ObjectAnimationUsingKeyFrames>
                                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="GlyphElement" Storyboard.TargetProperty="Foreground">
                                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlButtonForegroundPressed}"/>
                                                                    </ObjectAnimationUsingKeyFrames>
                                                                </Storyboard>
                                                            </VisualState>
                                                            <VisualState x:Name="Disabled">
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetName="ButtonLayoutGrid" Storyboard.TargetProperty="Opacity" To="0" Duration="0"/>
                                                                </Storyboard>
                                                            </VisualState>
                                                        </VisualStateGroup>
                                                    </VisualStateManager.VisualStateGroups>
                                                    <TextBlock x:Name="GlyphElement"
                                                               Foreground="{ThemeResource TextControlButtonForeground}"
                                                               VerticalAlignment="Center" HorizontalAlignment="Center"
                                                               FontStyle="Normal" FontSize="12"
                                                               Text="&#xE10A;"
                                                               FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                               AutomationProperties.AccessibilityView="Raw"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Grid.Resources>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Disabled">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentElement" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlForegroundDisabled}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBackgroundDisabled}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBorderBrushDisabled}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Normal">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="Transparent"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="Transparent"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBorderBrushPointerOver}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBackgroundPointerOver}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Focused">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBorderBrushFocused}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBackgroundFocused}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentElement" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlForegroundFocused}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="ButtonStates">
                                    <VisualState x:Name="ButtonVisible">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="DeleteButton" Storyboard.TargetProperty="Visibility">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="Visible"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="ButtonCollapsed"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="BorderElement"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{ThemeResource ControlCornerRadius}"
                                    Grid.ColumnSpan="2"/>
                            <ScrollViewer x:Name="ContentElement"
                                          HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
                                          HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                          VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
                                          VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                                          IsDeferredScrollingEnabled="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
                                          Margin="{TemplateBinding BorderThickness}"
                                          Padding="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          IsTabStop="False"
                                          AutomationProperties.AccessibilityView="Raw"
                                          ZoomMode="Disabled"/>
                            <ContentControl x:Name="PlaceholderTextContentPresenter"
                                            Foreground="{ThemeResource TextControlPlaceholderForeground}"
                                            Margin="{TemplateBinding BorderThickness}"
                                            Padding="{TemplateBinding Padding}"
                                            VerticalAlignment="Center"
                                            IsTabStop="False"
                                            Grid.ColumnSpan="2"
                                            Content="{TemplateBinding PlaceholderText}"
                                            IsHitTestVisible="False"/>
                            <Button x:Name="DeleteButton"
                                    Style="{StaticResource DeleteButtonStyle}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Margin="{ThemeResource HelperButtonThemePadding}"
                                    IsTabStop="False"
                                    Grid.Column="1"
                                    Visibility="Collapsed"
                                    AutomationProperties.AccessibilityView="Raw"
                                    FontSize="{TemplateBinding FontSize}"
                                    MinWidth="34"
                                    VerticalAlignment="Stretch"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AlwaysVisibleScrollViewerStyle" TargetType="ScrollViewer">
            <Setter Property="HorizontalScrollMode" Value="Enabled"/>
            <Setter Property="VerticalScrollMode" Value="Enabled"/>
            <Setter Property="IsHorizontalRailEnabled" Value="True"/>
            <Setter Property="IsVerticalRailEnabled" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="ZoomMode" Value="Enabled"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Visible"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="ScrollingIndicatorStates">
                                    <!-- 触摸时也用 MouseIndicator，保持滚动条始终展开 -->
                                    <VisualState x:Name="TouchIndicator">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Duration="0"
                                                Storyboard.TargetProperty="IndicatorMode"
                                                Storyboard.TargetName="VerticalScrollBar">
                                                <DiscreteObjectKeyFrame KeyTime="0">
                                                    <DiscreteObjectKeyFrame.Value>
                                                        <ScrollingIndicatorMode>MouseIndicator</ScrollingIndicatorMode>
                                                    </DiscreteObjectKeyFrame.Value>
                                                </DiscreteObjectKeyFrame>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Duration="0"
                                                Storyboard.TargetProperty="IndicatorMode"
                                                Storyboard.TargetName="HorizontalScrollBar">
                                                <DiscreteObjectKeyFrame KeyTime="0">
                                                    <DiscreteObjectKeyFrame.Value>
                                                        <ScrollingIndicatorMode>MouseIndicator</ScrollingIndicatorMode>
                                                    </DiscreteObjectKeyFrame.Value>
                                                </DiscreteObjectKeyFrame>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="MouseIndicator">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Duration="0"
                                                Storyboard.TargetProperty="IndicatorMode"
                                                Storyboard.TargetName="VerticalScrollBar">
                                                <DiscreteObjectKeyFrame KeyTime="0">
                                                    <DiscreteObjectKeyFrame.Value>
                                                        <ScrollingIndicatorMode>MouseIndicator</ScrollingIndicatorMode>
                                                    </DiscreteObjectKeyFrame.Value>
                                                </DiscreteObjectKeyFrame>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Duration="0"
                                                Storyboard.TargetProperty="IndicatorMode"
                                                Storyboard.TargetName="HorizontalScrollBar">
                                                <DiscreteObjectKeyFrame KeyTime="0">
                                                    <DiscreteObjectKeyFrame.Value>
                                                        <ScrollingIndicatorMode>MouseIndicator</ScrollingIndicatorMode>
                                                    </DiscreteObjectKeyFrame.Value>
                                                </DiscreteObjectKeyFrame>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Grid Background="{TemplateBinding Background}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <ScrollContentPresenter x:Name="ScrollContentPresenter"
                                    Grid.ColumnSpan="2" Grid.RowSpan="2"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    Margin="{TemplateBinding Padding}"/>
                                <ScrollBar x:Name="VerticalScrollBar"
                                    Grid.Column="1" Grid.Row="0"
                                    HorizontalAlignment="Right"
                                    IsTabStop="False"
                                    Maximum="{TemplateBinding ScrollableHeight}"
                                    Orientation="Vertical"
                                    Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                    Value="{TemplateBinding VerticalOffset}"
                                    ViewportSize="{TemplateBinding ViewportHeight}"/>
                                <ScrollBar x:Name="HorizontalScrollBar"
                                    Grid.Column="0" Grid.Row="1"
                                    IsTabStop="False"
                                    Maximum="{TemplateBinding ScrollableWidth}"
                                    Orientation="Horizontal"
                                    Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                    Value="{TemplateBinding HorizontalOffset}"
                                    ViewportSize="{TemplateBinding ViewportWidth}"/>
                                <Border x:Name="ScrollBarSeparator"
                                    Grid.Column="1" Grid.Row="1"
                                    Background="Transparent"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="TopToolbarRow"    Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition x:Name="BottomToolbarRow" Height="0"/>
        </Grid.RowDefinitions>

        <!-- 工具栏（学校模式下移至 Row=2） -->
        <Grid x:Name="Toolbar"
              Grid.Row="0"
              Visibility="Collapsed"
              Height="48"
              Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左组：目录 -->
            <StackPanel Grid.Column="0" Orientation="Horizontal"
                        VerticalAlignment="Center" HorizontalAlignment="Left"
                        Margin="8,0,0,0">
                <AppBarToggleButton x:Name="CatalogButton"
                                    Width="40"
                                    LabelPosition="Collapsed"
                                    Click="CatalogButton_Click">
                    <AppBarToggleButton.Icon>
                        <FontIcon Glyph="&#xE8A4;" />
                    </AppBarToggleButton.Icon>
                </AppBarToggleButton>
            </StackPanel>

            <!-- 中组：缩放控件 + 页数跳转，真正居中 -->
            <StackPanel Grid.Column="1" Orientation="Horizontal"
                        VerticalAlignment="Center" HorizontalAlignment="Center">
                <AppBarButton x:Name="ZoomOutButton" Icon="ZoomOut" x:Uid="ZoomOut"
                              Click="ZoomOutButton_Click" Width="40" LabelPosition="Collapsed"/>
                <AppBarButton x:Name="ZoomInButton"  Icon="ZoomIn"  x:Uid="ZoomIn"
                              Click="ZoomInButton_Click"  Width="40" LabelPosition="Collapsed"/>
                <AppBarButton x:Name="ZoomFitButton" Click="ZoomFitButton_Click"
                              Width="40" LabelPosition="Collapsed">
                    <AppBarButton.Icon>
                        <FontIcon x:Name="ZoomFitIcon" Glyph="&#xEAD6;"/>
                    </AppBarButton.Icon>
                </AppBarButton>

                <!-- 分隔线 -->
                <Border Width="1" Height="20" Margin="4,0"
                        Background="{ThemeResource AppBarSeparatorForegroundThemeBrush}"
                        VerticalAlignment="Center"/>

                <!-- 上一页 -->
                <AppBarButton x:Name="PrevPageButton"
                              Click="PrevPageButton_Click"
                              Width="40" LabelPosition="Collapsed">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xE76B;"/>
                    </AppBarButton.Icon>
                </AppBarButton>

                <!-- 页码输入框 / 总页数 -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="4">
                    <TextBox x:Name="PageNumberBox"
                             Style="{StaticResource CompactSquareTextBoxStyle}"
                             TextAlignment="Center"
                             VerticalAlignment="Center"
                             VerticalContentAlignment="Center"
                             InputScope="Number"
                             KeyDown="PageNumberBox_KeyDown"
                             LostFocus="PageNumberBox_LostFocus"/>
                    <TextBlock x:Name="TotalPagesText"
                               VerticalAlignment="Center"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>

                <!-- 下一页 -->
                <AppBarButton x:Name="NextPageButton"
                              Click="NextPageButton_Click"
                              Width="40" LabelPosition="Collapsed">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xE76C;"/>
                    </AppBarButton.Icon>
                </AppBarButton>

                <!-- 分隔线 -->
                <Border Width="1" Height="20" Margin="4,0"
                        Background="{ThemeResource AppBarSeparatorForegroundThemeBrush}"
                        VerticalAlignment="Center"/>

                <!-- 旋转 -->
                <AppBarButton x:Name="RotateButton"
                              Click="RotateButton_Click"
                              Width="40" LabelPosition="Collapsed">
                    <AppBarButton.Icon>
                        <FontIcon Glyph="&#xE7AD;"/>
                    </AppBarButton.Icon>
                </AppBarButton>
            </StackPanel>

            <!-- 右组：模式切换 -->
            <StackPanel Grid.Column="2" Orientation="Horizontal"
                        VerticalAlignment="Center" HorizontalAlignment="Right"
                        Margin="0,0,8,0">
                <AppBarToggleButton x:Name="LowEndToggle"
                                    x:Uid="LowEndMode"
                                    Click="LowEndToggle_Click"
                                    Width="40" LabelPosition="Collapsed">
                    <AppBarToggleButton.Icon>
                        <FontIcon Glyph="&#xE945;"/>
                    </AppBarToggleButton.Icon>
                </AppBarToggleButton>
            </StackPanel>
        </Grid>

        <!-- 工具栏分割线（顶部时在底部，底部时在顶部） -->
        <Border x:Name="ToolbarDivider"
                Grid.Row="0"
                VerticalAlignment="Bottom"
                Height="1"
                Visibility="{Binding Visibility, ElementName=Toolbar}"
                Background="{ThemeResource TabViewBorderBrush}"/>

        <SplitView x:Name="CatalogSplitView"
                   Grid.Row="1"
                   DisplayMode="Overlay"
                   CompactPaneLength="0"
                   OpenPaneLength="176"
                   PanePlacement="Left"
                   IsPaneOpen="False"
                   PaneBackground="{ThemeResource CatalogPaneAcrylicBrush}"
                   PaneClosed="CatalogSplitView_PaneClosed"
                   PaneOpened="CatalogSplitView_PaneOpened">
            <SplitView.Pane>
                <Grid x:Name="CatalogPane">
                    <Border Margin="0,0,6,0"
                            BorderThickness="0,0,1,0"
                            BorderBrush="{ThemeResource TabViewBorderBrush}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      Padding="8,0">
                            <StackPanel Margin="0,20">
                            <muxc:ItemsRepeater x:Name="ThumbnailsRepeater"
                                                HorizontalAlignment="Stretch">
                                <muxc:ItemsRepeater.Layout>
                                    <muxc:StackLayout Orientation="Vertical" Spacing="12"/>
                                </muxc:ItemsRepeater.Layout>
                                <muxc:ItemsRepeater.ItemTemplate>
                                    <DataTemplate x:DataType="local:PdfPageItem">
                                        <Button Click="ThumbnailButton_Click"
                                                Tag="{x:Bind PageIndex, Mode=OneWay}"
                                                HorizontalAlignment="Stretch"
                                                HorizontalContentAlignment="Center"
                                                Padding="0"
                                                Background="Transparent"
                                                BorderThickness="0">
                                            <StackPanel Width="136"
                                                        Spacing="6"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Top">
                                                <Border Width="136"
                                                        Height="176">
                                                    <Image Source="{x:Bind Thumbnail, Mode=OneWay}"
                                                           Stretch="Uniform"/>
                                                </Border>
                                                <TextBlock Text="{x:Bind PageNumber, Mode=OneWay}"
                                                           HorizontalAlignment="Center"
                                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                            </StackPanel>
                                        </Button>
                                    </DataTemplate>
                                </muxc:ItemsRepeater.ItemTemplate>
                            </muxc:ItemsRepeater>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                    <Grid x:Name="CatalogPaneResizeGrip"
                          Width="12"
                          HorizontalAlignment="Right"
                          Background="Transparent"
                          ToolTipService.ToolTip="调整目录宽度"
                          AutomationProperties.Name="调整目录宽度"
                          PointerEntered="CatalogPaneResizeGrip_PointerEntered"
                          PointerExited="CatalogPaneResizeGrip_PointerExited"
                          PointerPressed="CatalogPaneResizeGrip_PointerPressed"
                          PointerMoved="CatalogPaneResizeGrip_PointerMoved"
                          PointerReleased="CatalogPaneResizeGrip_PointerReleased"
                          PointerCanceled="CatalogPaneResizeGrip_PointerReleased"
                          PointerCaptureLost="CatalogPaneResizeGrip_PointerReleased">
                        <Border x:Name="CatalogPaneResizeGripIndicator"
                                Width="1"
                                HorizontalAlignment="Center"
                                Opacity="0"
                                Background="{ThemeResource SystemControlForegroundBaseMediumLowBrush}"/>
                    </Grid>
                </Grid>
            </SplitView.Pane>

            <SplitView.Content>
                <Grid>
                <!-- 加载进度环 -->
                <muxc:ProgressRing x:Name="LoadingRing"
                                   IsActive="False" Width="48" Height="48"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>

                <!-- PDF 内容 -->
                <ScrollViewer x:Name="PdfScrollViewer"
                              Style="{StaticResource AlwaysVisibleScrollViewerStyle}"
                              Visibility="Collapsed"
                              VerticalScrollBarVisibility="Visible"
                              HorizontalScrollBarVisibility="Auto"
                              ZoomMode="Enabled"
                              MinZoomFactor="0.25"
                              MaxZoomFactor="4.0"
                              IsDeferredScrollingEnabled="False"
                              SizeChanged="PdfScrollViewer_SizeChanged">
                    <Grid x:Name="PagesHost" HorizontalAlignment="Center">
                        <muxc:ItemsRepeater x:Name="PagesRepeater"
                                            HorizontalAlignment="Center">
                            <muxc:ItemsRepeater.Layout>
                                <muxc:StackLayout Orientation="Vertical" Spacing="16"/>
                            </muxc:ItemsRepeater.Layout>
                            <muxc:ItemsRepeater.ItemTemplate>
                                <DataTemplate x:DataType="local:PdfPageItem">
                                    <local:PdfPageView
                                        Width="{x:Bind DisplayWidth, Mode=OneWay}"
                                        Height="{x:Bind DisplayHeight, Mode=OneWay}"/>
                                </DataTemplate>
                            </muxc:ItemsRepeater.ItemTemplate>
                        </muxc:ItemsRepeater>
                    </Grid>
                </ScrollViewer>
                </Grid>
            </SplitView.Content>
        </SplitView>
    </Grid>
</Page>
